import LoginPage from "@/components/pages/login";
import React from "react";

type Props = {};

const Page = (props: Props) => {
  return (
    <>
      <LoginPage />
    </>
  );
};

export default Page;
